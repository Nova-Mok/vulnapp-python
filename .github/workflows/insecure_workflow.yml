name: Insecure Workflow

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2 

      - name: Run unpinned third-party action
        uses: madhead/semver-utils@v0.1.0 # Unpinned third-party action

      - name: Run a script
        run: echo "Hello, world!"
